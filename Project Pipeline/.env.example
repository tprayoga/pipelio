# Salin ke .env.local hanya bila perlu. Untuk pengembangan biasa file ini
# tidak dibutuhkan sama sekali — nilai defaultnya sudah benar.

# Alamat backend yang di-proxy Vite pada /api.
# Default: http://localhost:8000 (sesuai ./dev.sh)
# VITE_API_PROXY_TARGET=http://localhost:8000
